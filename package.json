{
  "name": "techfest-website",
  "version": "1.0.0",
  "description": "TechCarvaan 2025 - Technical Festival Website with Environment Variables",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server ${PORT:-8080}",
    "dev": "python -m http.server 3000",
    "build": "echo 'Building for production...' && cp -r . dist/ 2>/dev/null || robocopy . dist /E /XD node_modules .git dist > nul 2>&1 || true",
    "serve": "python -m http.server ${PORT:-8080}"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/VedantMeshram07/miniTech.git"
  },
  "keywords": [
    "techfest",
    "festival",
    "website",
    "firebase",
    "static"
  ],
  "author": "GCOEJ Technical Council",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
